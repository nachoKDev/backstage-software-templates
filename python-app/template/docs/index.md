# Documentation for python app

That is the documentation for python api created for test backstage

This application has two endpoints: 
 - `/api/v1/info`
 - `/api/v1/healthz`


# How to acces the app?

You can access the app by accessing al this URL: `${{values.app_name}}-${{values.app_env}}.test.com/api/v1/info`